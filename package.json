{
  "name": "currency-pairs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-all-deps": "concurrently \"npm install\" \"cd server && npm install\" \"cd client && npm install\"",
    "start": "concurrently \"cd server && npm start\" \"cd client && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
